# PostgreSQL connection
DATABASE_URL=postgres://postgres:postgres@localhost:5432/deskguard

# Server
PORT=4000

# Timer rules (minutes / seconds)
HEARTBEAT_INTERVAL_MIN=120
HEARTBEAT_GRACE_MIN=5
AWAY_MAX_MIN=20
SWEEP_INTERVAL_SEC=60
